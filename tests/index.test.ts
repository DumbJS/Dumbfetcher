import { DumbfetcherHooks, Dumbfetcher } from '../src/index';

describe('testing index file', () => {
    test('empty string should result in zero', () => {
        console.log(DumbfetcherHooks);
    });
});
